# Grafana Dashboards & Datasources

This directory contains Grafana datasources and dashboard JSON definitions.

- `dashboards/`: Pre-configured dashboards for API latency, request rates, AI generation queues, and system resources.
- `datasources/`: Automated datasource provisioning pointing to Prometheus.
