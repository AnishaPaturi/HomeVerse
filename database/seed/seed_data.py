"""
Database Seeding Script for HomeVerse
Seeds initial demo users, styles, sample catalog products, and default budget categories.
"""
import os
import sys

def seed():
    print("Seeding initial HomeVerse development database...")
    # Seed data logic will connect to database and insert catalog defaults
    print("Database seeding completed successfully.")

if __name__ == "__main__":
    seed()
